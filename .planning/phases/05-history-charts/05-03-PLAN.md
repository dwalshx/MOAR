---
phase: 05-history-charts
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/ExerciseDetailPage.tsx
  - src/components/exercise/ExerciseVolumeChart.tsx
  - src/components/exercise/SessionRow.tsx
  - src/components/workout/ExerciseCard.tsx
autonomous: true
requirements: [HST-02, FBK-04]

must_haves:
  truths:
    - "User can view per-exercise history showing each session's date, set count, and volume"
    - "User can tap a session row to expand and see individual sets"
    - "User can see a volume-over-time line chart for any exercise"
    - "Exercise names in workout detail and active workout are tappable links to exercise page"
  artifacts:
    - path: "src/pages/ExerciseDetailPage.tsx"
      provides: "Per-exercise history with chart and expandable sessions"
      min_lines: 50
    - path: "src/components/exercise/ExerciseVolumeChart.tsx"
      provides: "Recharts LineChart for per-exercise volume over time"
      min_lines: 20
    - path: "src/components/exercise/SessionRow.tsx"
      provides: "Expandable row showing session summary and individual sets"
      min_lines: 25
  key_links:
    - from: "src/pages/ExerciseDetailPage.tsx"
      to: "src/services/workoutService.ts"
      via: "useLiveQuery calling getExerciseHistory"
      pattern: "useLiveQuery.*getExerciseHistory"
    - from: "src/components/workout/ExerciseCard.tsx"
      to: "src/pages/ExerciseDetailPage.tsx"
      via: "navigate(/exercise/:name) on exercise name tap"
      pattern: "navigate.*exercise/"
---

<objective>
Build the ExerciseDetailPage (per-exercise history + volume chart) and make exercise names tappable links.

Purpose: HST-02 (exercise history across workouts) and FBK-04 (per-exercise volume chart) -- the exercise-level progress view.
Output: Functional exercise detail page, per-exercise chart, expandable session rows, tappable exercise name links.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-history-charts/05-CONTEXT.md
@.planning/phases/05-history-charts/05-RESEARCH.md
@.planning/phases/05-history-charts/05-01-SUMMARY.md
@src/pages/ExerciseDetailPage.tsx
@src/components/workout/ExerciseCard.tsx

<interfaces>
<!-- From Plan 01 (workoutService new exports) -->
```typescript
export interface ExerciseSession {
  workoutId: number;
  date: Date;
  setCount: number;
  totalVolume: number;
  sets: { setNumber: number; weight: number; reps: number }[];
}
export interface VolumeDataPoint {
  date: string; volume: number; fullDate: string;
}

// New method on workoutService:
getExerciseHistory(exerciseName: string, limit?: number): Promise<ExerciseSession[]>
```

<!-- From formatters.ts -->
```typescript
export function formatRelativeDate(date: Date): string;
export function formatVolume(totalLbs: number): string;
export function formatChartDate(date: Date): string;
```

<!-- ExerciseCard exercise name rendering (lines 106-107 collapsed, 133-134 expanded) -->
```typescript
// Collapsed:
<span className="font-semibold text-text-primary truncate max-w-[60%]">
  {exercise.exerciseName}
</span>
// Expanded:
<span className="font-semibold text-text-primary truncate max-w-[70%]">
  {exercise.exerciseName}
</span>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExerciseVolumeChart + SessionRow components</name>
  <files>src/components/exercise/ExerciseVolumeChart.tsx, src/components/exercise/SessionRow.tsx</files>
  <read_first>
    - .planning/phases/05-history-charts/05-RESEARCH.md (Recharts pattern, chart theme, expandable row pattern)
    - .planning/phases/05-history-charts/05-CONTEXT.md (D-14 through D-17, D-19 through D-25, D-27)
    - .planning/phases/05-history-charts/05-01-SUMMARY.md (service types)
  </read_first>
  <action>
    **ExerciseVolumeChart.tsx** (per D-15, D-19 through D-25, D-27, FBK-04):
    - Props: `data: VolumeDataPoint[]`
    - Nearly identical to WorkoutVolumeChart from Plan 02 — same Recharts config, same dark theme colors (#f97316 line, #a3a3a3 text, #333333 grid, #242424 tooltip bg)
    - If data.length < 2, show: "Complete more sessions to see your trend"
    - Wrap in div with `h-[200px]` (Pitfall 2)
    - ResponsiveContainer > LineChart > XAxis (dataKey="date") + YAxis + Tooltip (trigger="click", show fullDate via labelFormatter) + Line (type="monotone", dataKey="volume", stroke="#f97316", dot fill #f97316 r 4)
    - No area fill (D-20), one chart per view (D-24)

    **SessionRow.tsx** (per D-14, D-16):
    - Props: `session: ExerciseSession`, `isExpanded: boolean`, `onToggle: () => void`
    - Summary row (always visible): date (formatRelativeDate), set count, total volume (formatVolume)
    - Style: `bg-bg-card rounded-xl p-3 min-h-[44px]` (tap-friendly)
    - Tappable: onClick calls onToggle, cursor-pointer
    - Chevron icon on right that rotates when expanded (same pattern as ExerciseCard accordion)
    - When expanded: show sets table below the summary. Columns: Set#, Weight (lbs), Reps. Use text-sm text-text-secondary.
    - Expansion animated with simple conditional rendering (Claude's discretion on animation)
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "LineChart" src/components/exercise/ExerciseVolumeChart.tsx
    - grep -q "f97316" src/components/exercise/ExerciseVolumeChart.tsx
    - grep -q "ResponsiveContainer" src/components/exercise/ExerciseVolumeChart.tsx
    - grep -q "SessionRow" src/components/exercise/SessionRow.tsx
    - grep -q "isExpanded" src/components/exercise/SessionRow.tsx
    - grep -q "setNumber" src/components/exercise/SessionRow.tsx
  </acceptance_criteria>
  <done>ExerciseVolumeChart renders dark-themed line chart. SessionRow shows expandable session details.</done>
</task>

<task type="auto">
  <name>Task 2: ExerciseDetailPage + tappable exercise name links</name>
  <files>src/pages/ExerciseDetailPage.tsx, src/components/workout/ExerciseCard.tsx</files>
  <read_first>
    - src/pages/ExerciseDetailPage.tsx (placeholder to replace)
    - src/components/workout/ExerciseCard.tsx (exercise name spans to make tappable)
    - .planning/phases/05-history-charts/05-CONTEXT.md (D-13 through D-18, D-29)
    - src/components/exercise/ExerciseVolumeChart.tsx (just created)
    - src/components/exercise/SessionRow.tsx (just created)
  </read_first>
  <action>
    **ExerciseDetailPage.tsx** (per D-13, D-14, D-15, D-16, D-17, D-18):
    - Replace placeholder entirely
    - Get exercise name from useParams: `const { name } = useParams()` then `decodeURIComponent(name || '')`  (Pitfall 7)
    - Use useLiveQuery with `workoutService.getExerciseHistory(exerciseName, 20)`, dep: [exerciseName]
    - Transform sessions to VolumeDataPoint[] for chart: map each session to `{ date: formatChartDate(session.date), volume: session.totalVolume, fullDate: formatAbsoluteDate(session.date) }`. IMPORTANT: reverse the array (sessions come date-descending from service, chart needs chronological/oldest-first).
    - Page title: exercise name (D-17) — `text-2xl font-bold text-text-primary`
    - Back button at top (navigate -1)
    - ExerciseVolumeChart above the session table (D-15, D-27)
    - Session list: flex flex-col gap-2 of SessionRow components
    - Track expandedSessionId in state (by workoutId, not array index — same accordion pattern as ExerciseCard per established convention D-16)
    - Empty state: "No history for this exercise yet."

    **ExerciseCard.tsx** (per D-18, D-29):
    - Import useNavigate from 'react-router'
    - In the EXPANDED header (around line 133-134), make the exercise name span tappable:
      - Change from plain `<span>` to tappable span
      - Add `onClick` with `(e) => { e.stopPropagation(); navigate(`/exercise/${encodeURIComponent(exercise.exerciseName)}`); }`
      - e.stopPropagation() prevents the parent div's onToggle from firing
      - Add styling: `text-accent cursor-pointer active:opacity-80` (replacing text-text-primary)
    - In the COLLAPSED view (around line 106-107), do NOT make the exercise name tappable — the entire collapsed card is already an onToggle click target, and making the name a separate link would cause UX confusion. The user taps to expand first, then can tap the name link.
    - This gives users access to exercise history from the active workout screen (D-18)
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "getExerciseHistory" src/pages/ExerciseDetailPage.tsx
    - grep -q "ExerciseVolumeChart" src/pages/ExerciseDetailPage.tsx
    - grep -q "SessionRow" src/pages/ExerciseDetailPage.tsx
    - grep -q "expandedSession" src/pages/ExerciseDetailPage.tsx
    - grep -q "decodeURIComponent" src/pages/ExerciseDetailPage.tsx
    - grep -q "useNavigate" src/components/workout/ExerciseCard.tsx
    - grep -q "encodeURIComponent" src/components/workout/ExerciseCard.tsx
    - grep -q "exercise/" src/components/workout/ExerciseCard.tsx
    - grep -q "stopPropagation" src/components/workout/ExerciseCard.tsx
  </acceptance_criteria>
  <done>ExerciseDetailPage shows per-exercise history with chart and expandable sessions. Exercise names in active workout are tappable links to exercise detail.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit && npx vitest run --reporter=verbose
TypeScript compiles clean. All existing tests pass. No regressions.
</verification>

<success_criteria>
- /exercise/:name shows per-exercise history with volume chart (HST-02, FBK-04)
- Session rows expand to show individual sets on tap
- Exercise names in active workout ExerciseCard navigate to exercise detail
- Charts use orange accent line on dark background
</success_criteria>

<output>
After completion, create `.planning/phases/05-history-charts/05-03-SUMMARY.md`
</output>
