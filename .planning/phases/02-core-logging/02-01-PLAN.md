---
phase: 02-core-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/workoutService.ts
  - src/hooks/useLongPress.ts
  - src/hooks/useSwipeToDelete.ts
  - src/components/workout/Stepper.tsx
  - src/test-setup.ts
  - src/services/workoutService.test.ts
  - src/hooks/useLongPress.test.ts
  - vite.config.ts
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, LOG-05]

must_haves:
  truths:
    - "workoutService can start a workout, add exercises, log sets, edit sets, delete sets, finish a workout, and query last session values"
    - "Stepper component renders +/- buttons with long-press support for fine-grained adjustment"
    - "Swipe-to-delete hook tracks horizontal touch movement and exposes showDelete state"
    - "All service tests pass with fake-indexeddb"
  artifacts:
    - path: "src/services/workoutService.ts"
      provides: "All Dexie CRUD operations for workout logging"
      exports: ["workoutService"]
    - path: "src/hooks/useLongPress.ts"
      provides: "Tap vs long-press detection hook"
      exports: ["useLongPress"]
    - path: "src/hooks/useSwipeToDelete.ts"
      provides: "Swipe gesture detection hook"
      exports: ["useSwipeToDelete"]
    - path: "src/components/workout/Stepper.tsx"
      provides: "Reusable stepper with +/- buttons and long-press"
      exports: ["default"]
    - path: "src/services/workoutService.test.ts"
      provides: "Service layer tests"
    - path: "src/hooks/useLongPress.test.ts"
      provides: "Long-press hook tests"
  key_links:
    - from: "src/services/workoutService.ts"
      to: "src/db/database.ts"
      via: "import { db }"
      pattern: "import.*db.*from.*database"
    - from: "src/components/workout/Stepper.tsx"
      to: "src/hooks/useLongPress.ts"
      via: "import { useLongPress }"
      pattern: "import.*useLongPress"
---

<objective>
Build the service layer, reusable hooks, and Stepper component that form the foundation for the active workout screen. Also set up Vitest with fake-indexeddb and write tests for the service and long-press hook.

Purpose: All UI components in Plans 02 and 03 depend on workoutService for data operations and Stepper/hooks for interaction patterns. Building and testing these first ensures the UI layer has solid, tested foundations.

Output: Tested workoutService, useLongPress hook, useSwipeToDelete hook, Stepper component, and Vitest test infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-logging/02-CONTEXT.md
@.planning/phases/02-core-logging/02-RESEARCH.md

@src/db/models.ts
@src/db/database.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/models.ts:
```typescript
export interface Workout {
  id?: number;
  name: string;
  templateId?: number;
  startedAt: Date;
  completedAt?: Date;
  notes?: string;
}

export interface WorkoutExercise {
  id?: number;
  workoutId: number;
  exerciseName: string;
  order: number;
}

export interface WorkoutSet {
  id?: number;
  workoutExerciseId: number;
  setNumber: number;
  weight: number;
  reps: number;
  timestamp: Date;
}
```

From src/db/database.ts:
```typescript
export class MoarDatabase extends Dexie {
  workouts!: Table<Workout, number>;
  workoutExercises!: Table<WorkoutExercise, number>;
  workoutSets!: Table<WorkoutSet, number>;
  workoutTemplates!: Table<WorkoutTemplate, number>;
}
export const db = new MoarDatabase();
```

From src/index.css (Tailwind theme tokens):
```css
--color-bg-primary: #0f0f0f;
--color-bg-secondary: #1a1a1a;
--color-bg-card: #242424;
--color-accent: #f97316;
--color-accent-hover: #ea580c;
--color-text-primary: #f5f5f5;
--color-text-secondary: #a3a3a3;
--color-success: #22c55e;
--color-border: #333333;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest setup + WorkoutService with tests</name>
  <files>vite.config.ts, src/test-setup.ts, src/services/workoutService.ts, src/services/workoutService.test.ts</files>
  <read_first>src/db/models.ts, src/db/database.ts, vite.config.ts, .planning/phases/02-core-logging/02-RESEARCH.md</read_first>
  <behavior>
    - startWorkout(): creates a Workout with auto-generated name "Workout - {Mon D}" format, startedAt set, no completedAt, returns numeric id
    - addExercise(workoutId, name): normalizes name (trim + title-case per D-20), sets order incrementally, returns numeric id
    - logSet(workoutExerciseId, weight, reps): creates set with auto-incremented setNumber, timestamp set to current time (LOG-04), returns numeric id
    - updateSet(setId, weight, reps): updates existing set weight and reps (LOG-05)
    - deleteSet(setId): removes set from DB (LOG-05)
    - getActiveWorkout(): returns workout where completedAt is falsy (use .filter() not .where() — Dexie cannot index undefined/null per Pitfall #1)
    - finishWorkout(workoutId): sets completedAt to current time (D-26)
    - getLastSetValues(exerciseName): returns {weight, reps} from most recent set for this exercise name across all workouts; returns null if none found (LOG-02)
    - getExerciseNames(): returns unique exercise names from history for autocomplete (D-18)
    - normalizeExerciseName("bench press") returns "Bench Press", " squat  " returns "Squat"
  </behavior>
  <action>
    **Step 1: Configure Vitest.**
    Update vite.config.ts to add a `test` section:
    ```typescript
    test: {
      globals: true,
      environment: 'node',
      setupFiles: ['./src/test-setup.ts'],
    }
    ```

    **Step 2: Create test setup file.**
    Create src/test-setup.ts with:
    ```typescript
    import 'fake-indexeddb/auto';
    ```
    fake-indexeddb is already installed as a devDependency.

    **Step 3: Write failing tests FIRST (RED phase).**
    Create src/services/workoutService.test.ts covering all behaviors listed above. Use `beforeEach` to clear all tables via `db.workouts.clear()`, `db.workoutExercises.clear()`, `db.workoutSets.clear()`. Test groups:
    - "startWorkout" — creates workout with name format, startedAt set, no completedAt
    - "addExercise" — normalizes names, increments order
    - "logSet" — creates set with timestamp (LOG-04), increments setNumber
    - "updateSet" — changes weight/reps (LOG-05)
    - "deleteSet" — removes set (LOG-05)
    - "getActiveWorkout" — finds workout without completedAt, returns undefined when none active
    - "finishWorkout" — sets completedAt (D-26)
    - "getLastSetValues" — returns last values or null (LOG-02)
    - "getExerciseNames" — returns unique names (D-18)

    Run tests: `npx vitest run src/services/workoutService.test.ts` — all should FAIL.

    **Step 4: Implement workoutService (GREEN phase).**
    Create src/services/workoutService.ts as a plain object with methods matching the research code examples. Key implementation notes:
    - Import `db` from `../db/database` and types from `../db/models`
    - `getActiveWorkout()`: use `db.workouts.filter(w => !w.completedAt).first()` — NOT `.where('completedAt')` (Pitfall #1)
    - `normalizeExerciseName()`: export as a named function for direct testing — `trim().split(/\s+/).map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ')`
    - `getLastSetValues()`: query workoutExercises by exerciseName, get their IDs, query workoutSets by those IDs, sort by timestamp descending, return first result's weight/reps
    - `logSet()`: always set `timestamp: new Date()` per LOG-04
    - `getExerciseNames()`: use `db.workoutExercises.orderBy('exerciseName').uniqueKeys()` cast to string[]

    Run tests: `npx vitest run src/services/workoutService.test.ts` — all should PASS.
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx vitest run src/services/workoutService.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const workoutService" src/services/workoutService.ts
    - grep -q "export function normalizeExerciseName" src/services/workoutService.ts
    - grep -q "filter.*completedAt" src/services/workoutService.ts
    - grep -q "timestamp.*new Date" src/services/workoutService.ts
    - grep -q "fake-indexeddb/auto" src/test-setup.ts
    - grep -q "setupFiles" vite.config.ts
    - grep -q "startWorkout" src/services/workoutService.test.ts
    - grep -q "getLastSetValues" src/services/workoutService.test.ts
    - grep -q "deleteSet" src/services/workoutService.test.ts
  </acceptance_criteria>
  <done>All workoutService tests pass. Service exports startWorkout, addExercise, logSet, updateSet, deleteSet, getActiveWorkout, finishWorkout, getLastSetValues, getExerciseNames, normalizeExerciseName. Vitest configured with fake-indexeddb.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useLongPress hook with tests + useSwipeToDelete hook</name>
  <files>src/hooks/useLongPress.ts, src/hooks/useLongPress.test.ts, src/hooks/useSwipeToDelete.ts</files>
  <read_first>src/test-setup.ts, .planning/phases/02-core-logging/02-RESEARCH.md</read_first>
  <behavior>
    - useLongPress: onTap fires on quick press-release (under threshold), onLongPress fires after threshold (400ms default), auto-repeats on sustained hold every 150ms
    - useSwipeToDelete: tracks horizontal touch distance, exposes offset/showDelete/reset, showDelete triggers when swipe exceeds 60% of threshold
  </behavior>
  <action>
    **Step 1: Write useLongPress test (RED).**
    Create src/hooks/useLongPress.test.ts:
    - Test: "calls onTap for quick press" — simulate start then immediate stop, verify onTap called
    - Test: "calls onLongPress after threshold" — simulate start, advance timers past 400ms, verify onLongPress called
    - Test: "auto-repeats on sustained hold" — advance timers past threshold + 300ms, verify onLongPress called multiple times
    - Use `vi.useFakeTimers()` and `vi.advanceTimersByTime()`.
    - Import the hook and test by calling it with mock callbacks, then calling the returned event handlers directly.

    Run: `npx vitest run src/hooks/useLongPress.test.ts` — should FAIL.

    **Step 2: Implement useLongPress (GREEN).**
    Create src/hooks/useLongPress.ts following the research pattern exactly:
    - Parameters: `onTap: () => void`, `onLongPress: () => void`, `threshold = 400`
    - Uses `useRef` for timer and interval refs, `useCallback` for start/stop
    - `start`: sets timeout for threshold ms, on trigger calls onLongPress and starts 150ms interval
    - `stop`: clears timeout and interval, if not long-press then calls onTap
    - Returns `{ onTouchStart: start, onTouchEnd: stop, onMouseDown: start, onMouseUp: stop }`

    Run: `npx vitest run src/hooks/useLongPress.test.ts` — should PASS.

    **Step 3: Implement useSwipeToDelete.**
    Create src/hooks/useSwipeToDelete.ts following the research pattern:
    - Parameters: `threshold = 80`
    - Tracks `startX` via useRef, `offset` and `showDelete` via useState
    - `onTouchStart`: records clientX
    - `onTouchMove`: calculates diff, sets offset (clamped 0 to threshold)
    - `onTouchEnd`: if offset >= threshold * 0.6, snap to threshold and set showDelete true; else reset
    - `reset()`: sets offset to 0, showDelete to false
    - Returns `{ offset, showDelete, onTouchStart, onTouchMove, onTouchEnd, reset }`

    No separate test for this hook (touch event simulation is low value; will be tested via component checkpoint in Plan 03).
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx vitest run src/hooks/useLongPress.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function useLongPress" src/hooks/useLongPress.ts
    - grep -q "onTouchStart" src/hooks/useLongPress.ts
    - grep -q "onLongPress" src/hooks/useLongPress.ts
    - grep -q "export function useSwipeToDelete" src/hooks/useSwipeToDelete.ts
    - grep -q "showDelete" src/hooks/useSwipeToDelete.ts
    - grep -q "useFakeTimers\|fakeTimers" src/hooks/useLongPress.test.ts
  </acceptance_criteria>
  <done>useLongPress hook passes all tests (tap detection, long-press detection, auto-repeat). useSwipeToDelete hook exports correctly with offset, showDelete, touch handlers, and reset.</done>
</task>

<task type="auto">
  <name>Task 3: Stepper component with long-press integration</name>
  <files>src/components/workout/Stepper.tsx</files>
  <read_first>src/hooks/useLongPress.ts, src/index.css</read_first>
  <action>
    Create src/components/workout/Stepper.tsx — a reusable numeric stepper with +/- buttons.

    **Props interface:**
    ```typescript
    interface StepperProps {
      value: number;
      onTapIncrement: number;    // e.g., 5 for weight, 1 for reps
      onLongPressIncrement: number; // e.g., 1 for weight (fine adjust), not used for reps
      onChange: (newValue: number) => void;
      min?: number;              // default 0
      label?: string;            // e.g., "lbs" or "reps"
    }
    ```

    **Implementation per D-08 and D-09:**
    - Minus button (left): tap decrements by `onTapIncrement`, long-press decrements by `onLongPressIncrement`
    - Plus button (right): tap increments by `onTapIncrement`, long-press increments by `onLongPressIncrement`
    - Value display in center with optional label below
    - Use `useLongPress` hook for both buttons
    - Enforce `min` constraint (value cannot go below min)
    - Add `touch-action: manipulation` CSS class to prevent double-tap zoom (Pitfall #4)
    - Call `e.preventDefault()` in touch handlers to prevent context menu on long-press

    **Styling (per dark theme tokens):**
    - Buttons: `min-w-[44px] min-h-[44px]` for touch targets, `bg-bg-card rounded-lg text-text-primary text-xl font-bold`
    - Active state: `active:bg-accent-hover` for tap feedback
    - Value: `text-2xl font-bold text-text-primary` centered
    - Label: `text-xs text-text-secondary` below value
    - Layout: flex row with gap-3, items-center

    For weight stepper usage: `onTapIncrement={5} onLongPressIncrement={1}`
    For reps stepper usage: `onTapIncrement={1} onLongPressIncrement={1}`
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit src/components/workout/Stepper.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useLongPress" src/components/workout/Stepper.tsx
    - grep -q "onTapIncrement" src/components/workout/Stepper.tsx
    - grep -q "onLongPressIncrement" src/components/workout/Stepper.tsx
    - grep -q "touch-action" src/components/workout/Stepper.tsx
    - grep -q "min-w-\[44px\]\|min-h-\[44px\]" src/components/workout/Stepper.tsx
    - grep -q "onChange" src/components/workout/Stepper.tsx
  </acceptance_criteria>
  <done>Stepper component renders +/- buttons with 44px touch targets, integrates useLongPress for tap vs long-press differentiation, supports configurable increment values, uses dark theme tokens, TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- All workoutService tests pass: `npx vitest run src/services/workoutService.test.ts`
- useLongPress tests pass: `npx vitest run src/hooks/useLongPress.test.ts`
- Full test suite: `npx vitest run`
- TypeScript compiles: `npx tsc --noEmit`
- No new dependencies needed (all already installed)
</verification>

<success_criteria>
- workoutService handles all CRUD operations for workouts, exercises, and sets
- Tests verify service behavior with fake-indexeddb
- useLongPress hook correctly differentiates tap from long-press
- useSwipeToDelete hook tracks horizontal swipe gesture
- Stepper component renders accessible +/- buttons with long-press support
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-logging/02-01-SUMMARY.md`
</output>
