---
phase: 02-core-logging
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/pages/WorkoutPage.tsx
  - src/pages/HomePage.tsx
  - src/components/layout/BottomNav.tsx
autonomous: false
requirements: [LOG-01, LOG-04, LOG-06]

must_haves:
  truths:
    - "Home screen shows a Start Workout button that creates a workout and navigates to it (D-13, D-14)"
    - "WorkoutPage loads ActiveWorkout component with the workout ID from the URL (D-15)"
    - "On app reload with an active workout, user is redirected to the active workout (D-25)"
    - "Finishing a workout navigates back to Home (D-26)"
    - "Bottom nav shows Workout tab with orange accent when a workout is active (D-27)"
    - "User can complete a full flow: start workout, add exercise, log set, finish workout"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Start Workout button + active workout redirect"
      exports: ["default"]
    - path: "src/pages/WorkoutPage.tsx"
      provides: "Active workout screen with ActiveWorkout component"
      exports: ["default"]
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Conditional Workout tab in bottom navigation"
      exports: ["default"]
  key_links:
    - from: "src/pages/HomePage.tsx"
      to: "src/services/workoutService.ts"
      via: "startWorkout() on button tap"
      pattern: "workoutService.*startWorkout"
    - from: "src/pages/HomePage.tsx"
      to: "/workout/:id"
      via: "useNavigate after creating workout"
      pattern: "navigate.*workout"
    - from: "src/pages/WorkoutPage.tsx"
      to: "src/components/workout/ActiveWorkout.tsx"
      via: "renders ActiveWorkout with workout ID"
      pattern: "ActiveWorkout"
    - from: "src/components/layout/BottomNav.tsx"
      to: "src/db/database.ts"
      via: "useLiveQuery for active workout detection"
      pattern: "useLiveQuery.*filter.*completedAt"
---

<objective>
Wire the workout UI components into the page routes and navigation. Replace placeholder pages with functional screens, add active workout detection for recovery on reload, and add the conditional Workout tab to the bottom nav.

Purpose: This is the integration layer that makes the full workout flow functional end-to-end. After this plan, a user can start a workout from Home, add exercises, log sets, and finish — the complete Phase 2 experience.

Output: Updated HomePage, WorkoutPage, and BottomNav that deliver the full active workout flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-logging/02-CONTEXT.md
@.planning/phases/02-core-logging/02-RESEARCH.md
@.planning/phases/02-core-logging/02-01-SUMMARY.md
@.planning/phases/02-core-logging/02-02-SUMMARY.md

@src/App.tsx
@src/db/models.ts
@src/db/database.ts
@src/index.css

<interfaces>
<!-- Existing page/layout contracts -->

From src/App.tsx:
```typescript
<Route index element={<HomePage />} />
<Route path="workout/:id" element={<WorkoutPage />} />
```

From src/components/workout/ActiveWorkout.tsx (created in Plan 02):
```typescript
interface ActiveWorkoutProps {
  workoutId: number;
}
export default function ActiveWorkout(props: ActiveWorkoutProps): JSX.Element;
```

From src/services/workoutService.ts (created in Plan 01):
```typescript
export const workoutService = {
  startWorkout(): Promise<number>,
  getActiveWorkout(): Promise<Workout | undefined>,
  finishWorkout(workoutId: number): Promise<void>,
};
```

From src/components/layout/BottomNav.tsx (existing):
```typescript
// Current: Home and History nav links
// Needs: conditional Workout link when active workout exists
```

From src/components/layout/AppLayout.tsx:
```typescript
// Layout: dark flex col h-dvh, main with overflow-y-auto px-4 pb-4
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomePage with Start Workout + active workout redirect</name>
  <files>src/pages/HomePage.tsx</files>
  <read_first>src/pages/HomePage.tsx, src/services/workoutService.ts, src/db/database.ts, src/App.tsx</read_first>
  <action>
    Replace the HomePage placeholder with a functional home screen.

    **Imports:** `useNavigate` from `react-router`, `useLiveQuery` from `dexie-react-hooks`, `db` from `../db/database`, `workoutService` from `../services/workoutService`.

    **Active workout detection (D-25):**
    Use `useLiveQuery(() => db.workouts.filter(w => !w.completedAt).first())` to detect any incomplete workout. If found, show a "Resume Workout" card/button that navigates to `/workout/${activeWorkout.id}`.

    Use `useEffect` for auto-redirect: if an active workout exists and user lands on home, do NOT auto-redirect (let them choose). Instead, show the resume option prominently. This is friendlier than a forced redirect.

    **Start Workout button (D-13, D-14):**
    Prominent button centered on the page: "Start Workout". On tap:
    ```typescript
    const handleStartWorkout = async () => {
      const workoutId = await workoutService.startWorkout();
      navigate(`/workout/${workoutId}`);
    };
    ```
    This creates a workout with auto-generated name "Workout - Apr 9" format (D-14) and navigates to it (D-15).

    **Layout:**
    - App title "MOAR" at top: `text-3xl font-bold text-text-primary pt-6`
    - Subtitle: `text-text-secondary mt-1 mb-8` — "Track your gains"
    - If active workout exists: "Resume Workout" card above the start button
      - Card: `bg-bg-card rounded-xl p-4 mb-4 flex items-center justify-between`
      - Left: workout name + "In progress" in `text-accent text-sm`
      - Right: arrow or "Resume" button in `text-accent font-semibold`
      - Tap navigates to `/workout/${activeWorkout.id}`
    - Start Workout button: `w-full bg-accent text-white font-bold py-4 rounded-xl text-lg min-h-[44px] active:bg-accent-hover transition-colors`
    - If active workout exists, Start Workout button becomes secondary styled: `bg-bg-card border border-border text-text-primary` with label "Start New Workout" (to differentiate from resume)
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Start Workout\|Start New Workout" src/pages/HomePage.tsx
    - grep -q "workoutService.startWorkout" src/pages/HomePage.tsx
    - grep -q "useNavigate" src/pages/HomePage.tsx
    - grep -q "useLiveQuery" src/pages/HomePage.tsx
    - grep -q "filter.*completedAt" src/pages/HomePage.tsx
    - grep -q "Resume" src/pages/HomePage.tsx
  </acceptance_criteria>
  <done>HomePage shows Start Workout button that creates a workout and navigates to /workout/:id. When an active workout exists, a Resume Workout card appears prominently. No forced auto-redirect.</done>
</task>

<task type="auto">
  <name>Task 2: WorkoutPage integration + BottomNav conditional Workout tab</name>
  <files>src/pages/WorkoutPage.tsx, src/components/layout/BottomNav.tsx</files>
  <read_first>src/pages/WorkoutPage.tsx, src/components/layout/BottomNav.tsx, src/components/workout/ActiveWorkout.tsx, src/services/workoutService.ts, src/App.tsx, src/db/database.ts</read_first>
  <action>
    **WorkoutPage.tsx — replace placeholder (D-15):**

    Replace entire file content. Import `useParams` and `useNavigate` from `react-router`, `useLiveQuery` from `dexie-react-hooks`, `db` from `../db/database`, `ActiveWorkout` from `../components/workout/ActiveWorkout`.

    Extract `id` from params via `useParams()`. Parse to number. If invalid (NaN), navigate to `/` with replace.

    Render `<ActiveWorkout workoutId={parsedId} />`.

    Handle the "finish" flow: ActiveWorkout's WorkoutHeader calls `workoutService.finishWorkout()`. After the workout's `completedAt` is set, the useLiveQuery in ActiveWorkout will detect it. Handle navigation in WorkoutPage by detecting when the workout is completed:

    ```typescript
    const workout = useLiveQuery(() => db.workouts.get(parsedId), [parsedId]);
    useEffect(() => {
      if (workout && workout.completedAt) {
        navigate('/', { replace: true });
      }
    }, [workout, navigate]);
    ```

    This way, when finishWorkout sets completedAt, useLiveQuery fires, and WorkoutPage auto-navigates home (D-26).

    ---

    **BottomNav.tsx — add conditional Workout tab (D-27):**

    Add imports: `useLiveQuery` from `dexie-react-hooks`, `db` from `../../db/database`.

    Detect active workout:
    ```typescript
    const activeWorkout = useLiveQuery(
      () => db.workouts.filter(w => !w.completedAt).first()
    );
    ```

    Between the Home and History nav links, conditionally render a Workout tab:
    ```tsx
    {activeWorkout && (
      <NavLink to={`/workout/${activeWorkout.id}`} className={linkClass}>
        <span className="text-lg mb-0.5">{'\u{1F3CB}'}</span>
        Workout
      </NavLink>
    )}
    ```

    The existing `linkClass` function already applies `text-accent` when active, so the orange accent (D-27) is handled automatically by React Router's `isActive` prop.

    Keep the existing Home and History links unchanged. Just insert the Workout link between them.
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ActiveWorkout" src/pages/WorkoutPage.tsx
    - grep -q "useParams" src/pages/WorkoutPage.tsx
    - grep -q "completedAt" src/pages/WorkoutPage.tsx
    - grep -q "navigate.*replace" src/pages/WorkoutPage.tsx
    - grep -q "useLiveQuery" src/components/layout/BottomNav.tsx
    - grep -q "activeWorkout" src/components/layout/BottomNav.tsx
    - grep -q "Workout" src/components/layout/BottomNav.tsx
    - grep -q "filter.*completedAt" src/components/layout/BottomNav.tsx
  </acceptance_criteria>
  <done>WorkoutPage renders ActiveWorkout with workout ID from URL params, auto-navigates home when workout is finished. BottomNav shows conditional Workout tab with orange accent when an active workout exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full workout flow verification</name>
  <files>src/pages/HomePage.tsx, src/pages/WorkoutPage.tsx, src/components/layout/BottomNav.tsx</files>
  <action>
    Human verification checkpoint. Run `npm run dev` and open http://localhost:5173 on your phone or mobile-responsive browser. Verify the complete workout flow:

    1. **Start:** Tap "Start Workout" on Home — should create workout and navigate to active workout screen
    2. **Add exercise:** Type "Bench Press" in the input at top, tap Enter or autocomplete suggestion — card should appear
    3. **Log set:** Card should auto-expand. Adjust weight with +/- (tap for 5lb, long-press for 1lb), adjust reps. Tap "Log Set"
    4. **Verify set appears:** Set should show in the card as "Set 1 | 135 lbs | 10 reps" (or whatever you logged)
    5. **Log another set:** Values should still be pre-filled. Tap Log Set again. Set 2 appears.
    6. **Add another exercise:** Type another exercise name. Previous card should collapse (accordion). New card expands.
    7. **Edit set:** Go back to first exercise, tap a logged set row. Steppers should appear for editing. Change values and save.
    8. **Delete set:** Swipe left on a set row. Delete button should appear. Tap to delete.
    9. **Bottom nav:** Workout tab should be visible with orange accent
    10. **Reload app:** Refresh the page. Should show Resume Workout on Home. Tap it — workout should load with all your data intact.
    11. **Finish:** Tap "Finish" button. Should return to Home. Workout tab should disappear from nav.

    Expected: All 11 steps work smoothly. Set logging feels fast (1-3 taps). Dark theme throughout.

    Resume signal: Type "approved" or describe specific issues to fix.
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles without errors
    - All vitest tests pass
    - User confirms full workout flow works on mobile
  </acceptance_criteria>
  <done>User approves the full workout flow after manual testing. All verification steps pass. Phase 2 core logging is functional.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- All tests pass: `npx vitest run`
- Full end-to-end flow works on mobile browser
- Active workout survives page reload
- Bottom nav Workout tab appears/disappears correctly
</verification>

<success_criteria>
- User can start a workout from Home in one tap (LOG-01, D-13)
- User can add exercises with autocomplete (D-17, D-18)
- User can log sets with stepper buttons in 1-3 taps (LOG-01, LOG-03)
- Sets are pre-filled from last session values (LOG-02)
- Every set is timestamped and persisted immediately (LOG-04, D-24)
- User can edit and delete sets (LOG-05)
- Exercise cards work as accordion (LOG-06, D-03)
- Active workout survives reload (D-25)
- Finishing returns to home (D-26)
- Workout tab appears in nav when active (D-27)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-logging/02-03-SUMMARY.md`
</output>
