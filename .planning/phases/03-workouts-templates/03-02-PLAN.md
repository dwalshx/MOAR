---
phase: 03-workouts-templates
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/workout/WorkoutHeader.tsx
  - src/components/workout/ActiveWorkout.tsx
autonomous: true
requirements: [WRK-01, WRK-03, WRK-04, WRK-05, WRK-06]

must_haves:
  truths:
    - "User can tap the workout name in the header and edit it inline"
    - "Edited name is saved to IndexedDB after 500ms debounce"
    - "Finishing a workout creates/updates a template with the workout's exercises"
    - "Active workout recovery restores the workout name (WRK-06)"
    - "Adding exercises mid-workout still works (WRK-04)"
  artifacts:
    - path: "src/components/workout/WorkoutHeader.tsx"
      provides: "Editable name input with debounced save"
      contains: "updateWorkoutName"
    - path: "src/components/workout/ActiveWorkout.tsx"
      provides: "Finish flow that triggers template upsert via updated finishWorkout"
  key_links:
    - from: "src/components/workout/WorkoutHeader.tsx"
      to: "src/services/workoutService.ts"
      via: "workoutService.updateWorkoutName on debounced input change"
      pattern: "updateWorkoutName"
    - from: "src/components/workout/ActiveWorkout.tsx"
      to: "src/services/workoutService.ts"
      via: "workoutService.finishWorkout (which now includes upsertTemplate)"
      pattern: "finishWorkout"
---

<objective>
Make WorkoutHeader's name field editable with debounced save, completing the naming flow. The finishWorkout -> template upsert is already wired in Plan 01's service layer changes; this plan ensures the UI triggers it correctly.

Purpose: Users can name their workouts inline (WRK-01, WRK-05) and templates auto-update on finish (WRK-03).
Output: Editable WorkoutHeader, verified template creation on finish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-workouts-templates/03-CONTEXT.md
@.planning/phases/03-workouts-templates/03-RESEARCH.md
@.planning/phases/03-workouts-templates/03-01-SUMMARY.md

@src/components/workout/WorkoutHeader.tsx
@src/components/workout/ActiveWorkout.tsx
@src/services/workoutService.ts
@src/db/models.ts

<interfaces>
<!-- From Plan 01 (service layer) -->
From src/services/workoutService.ts (new methods added in Plan 01):
```typescript
// Added to workoutService:
updateWorkoutName(workoutId: number, name: string): Promise<void>
// finishWorkout now internally calls upsertTemplate — no UI change needed for template creation
```

From src/db/models.ts:
```typescript
export interface Workout {
  id?: number;
  name: string;
  templateId?: number;
  startedAt: Date;
  completedAt?: Date;
  notes?: string;
}
```

Current WorkoutHeader (to be modified):
```typescript
interface WorkoutHeaderProps {
  workout: Workout;
  onFinish: () => void;
}
// Currently renders workout.name as static <h1>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make WorkoutHeader name editable with debounced save</name>
  <files>src/components/workout/WorkoutHeader.tsx</files>
  <read_first>src/components/workout/WorkoutHeader.tsx, src/services/workoutService.ts, src/db/models.ts</read_first>
  <action>
    Replace the static h1 workout name in WorkoutHeader.tsx with an editable input field. Per D-01, D-03, D-04:

    1. Add React imports: useState, useCallback, useRef, useEffect.
    2. Import workoutService from '../../services/workoutService'.
    3. Add local state: `const [name, setName] = useState(workout.name)`.
    4. Add useEffect to sync name from prop when it changes externally (recovery scenario D-20): `useEffect(() => { setName(workout.name); }, [workout.name])`.
    5. Add debounce ref: `const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)`.
    6. Add handleNameChange callback: sets local name state, clears previous timer, sets new 500ms timer that calls `workoutService.updateWorkoutName(workout.id!, newName)`.
    7. Add cleanup useEffect to clear timer on unmount.
    8. Replace the `<h1>` with an `<input type="text">` element:
       - value={name}
       - onChange={(e) => handleNameChange(e.target.value)}
       - className: "text-xl font-bold text-text-primary bg-transparent border-b border-transparent focus:border-accent outline-none truncate mr-4 min-w-0 flex-1"
       - placeholder="Workout name"
    9. Keep the Finish button exactly as-is.

    DO NOT use contentEditable — use a regular input per research anti-patterns.
    DO NOT add a separate save button — debounce handles persistence per D-03.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "updateWorkoutName" src/components/workout/WorkoutHeader.tsx
    - grep -q "useState" src/components/workout/WorkoutHeader.tsx
    - grep -q "useRef" src/components/workout/WorkoutHeader.tsx
    - grep -q "useCallback" src/components/workout/WorkoutHeader.tsx
    - grep -q 'type="text"' src/components/workout/WorkoutHeader.tsx
    - grep -q "setTimeout" src/components/workout/WorkoutHeader.tsx
    - grep -q "clearTimeout" src/components/workout/WorkoutHeader.tsx
    - grep -q "500" src/components/workout/WorkoutHeader.tsx
  </acceptance_criteria>
  <done>WorkoutHeader renders an editable input for workout name. Typing triggers a 500ms debounced save to IndexedDB via workoutService.updateWorkoutName. Name syncs from prop on external change. Finish button unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify ActiveWorkout finish flow triggers template upsert and WRK-04/WRK-06 still work</name>
  <files>src/components/workout/ActiveWorkout.tsx</files>
  <read_first>src/components/workout/ActiveWorkout.tsx, src/services/workoutService.ts</read_first>
  <action>
    The ActiveWorkout component already calls `workoutService.finishWorkout(workoutId)` in its handleFinish. Plan 01 modified finishWorkout to internally call upsertTemplate. So template creation on finish is already wired — no code change needed in ActiveWorkout for WRK-03.

    Verify the following by reading the code:
    1. ActiveWorkout.handleFinish calls workoutService.finishWorkout(workoutId) — confirms WRK-03 template upsert triggers.
    2. ActiveWorkout passes workout prop to WorkoutHeader — confirms WRK-06 name recovery (useLiveQuery feeds workout from DB, name is already persisted per D-20).
    3. ExerciseInput and addExercise still work — confirms WRK-04.
    4. The workout is fetched via useLiveQuery(() => db.workouts.get(workoutId)) — confirms it reacts to name changes made by WorkoutHeader.

    If ActiveWorkout already satisfies all the above with no changes needed, document this as a verification-only task. If any wiring is missing, make the minimal fix.

    Run the full test suite to confirm no regressions from WorkoutHeader changes.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "finishWorkout" src/components/workout/ActiveWorkout.tsx
    - grep -q "WorkoutHeader" src/components/workout/ActiveWorkout.tsx
    - grep -q "ExerciseInput" src/components/workout/ActiveWorkout.tsx
    - grep -q "useLiveQuery" src/components/workout/ActiveWorkout.tsx
  </acceptance_criteria>
  <done>ActiveWorkout finish flow triggers template upsert (via finishWorkout). Name recovery works via useLiveQuery feeding WorkoutHeader. ExerciseInput still wired for mid-workout adds. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite passes
- WorkoutHeader renders an input (not h1) for workout name
- Typing in the name field debounce-saves to IndexedDB
- Finishing a workout creates a template (confirmed via Plan 01's service tests)
- No changes needed to ActiveWorkout for template wiring
</verification>

<success_criteria>
- User can tap and edit the workout name inline in the header
- Name persists to DB after 500ms debounce
- Finishing a workout auto-creates/updates a template (service layer handles this)
- All existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/03-workouts-templates/03-02-SUMMARY.md`
</output>
