---
phase: 03-workouts-templates
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/home/RecentWorkouts.tsx
  - src/components/home/RecentWorkoutCard.tsx
  - src/pages/HomePage.tsx
autonomous: false
requirements: [WRK-02, WRK-05]

must_haves:
  truths:
    - "Home screen shows recent completed workouts below the Start button (D-16)"
    - "Each recent workout card shows name, relative date, and total volume (D-17)"
    - "Each card has a Repeat button that creates a new workout with the same exercises (D-06, D-07)"
    - "After tapping Repeat, user lands on active workout screen with exercises pre-loaded (D-09)"
    - "Start New Workout button remains prominent at top (D-15)"
    - "Resume Workout card still appears at top when active workout exists (D-19)"
  artifacts:
    - path: "src/components/home/RecentWorkouts.tsx"
      provides: "Recent workouts list using useLiveQuery + workoutService.getRecentWorkouts"
      contains: "useLiveQuery"
    - path: "src/components/home/RecentWorkoutCard.tsx"
      provides: "Individual card with name, date, volume, Repeat button"
      contains: "Repeat"
    - path: "src/pages/HomePage.tsx"
      provides: "Updated home page with RecentWorkouts integrated"
      contains: "RecentWorkouts"
  key_links:
    - from: "src/components/home/RecentWorkouts.tsx"
      to: "src/services/workoutService.ts"
      via: "workoutService.getRecentWorkouts and startWorkoutFromTemplate"
      pattern: "getRecentWorkouts"
    - from: "src/components/home/RecentWorkoutCard.tsx"
      to: "src/utils/formatters.ts"
      via: "formatRelativeDate and formatVolume for display"
      pattern: "formatRelativeDate|formatVolume"
    - from: "src/pages/HomePage.tsx"
      to: "src/components/home/RecentWorkouts.tsx"
      via: "Component import and render below start button"
      pattern: "RecentWorkouts"
---

<objective>
Build the Recent Workouts list on the home screen with one-tap Repeat functionality. Users see their last 10 workouts and can instantly start a new session from any of them.

Purpose: This is the primary fast-path for returning users (WRK-02). Instead of starting from scratch, one tap loads their previous workout's exercises with last session's numbers pre-filled.
Output: RecentWorkouts component, RecentWorkoutCard component, updated HomePage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-workouts-templates/03-CONTEXT.md
@.planning/phases/03-workouts-templates/03-RESEARCH.md
@.planning/phases/03-workouts-templates/03-01-SUMMARY.md

@src/pages/HomePage.tsx
@src/services/workoutService.ts
@src/utils/formatters.ts
@src/db/models.ts

<interfaces>
<!-- From Plan 01 (service layer) -->
From src/services/workoutService.ts (new methods from Plan 01):
```typescript
export interface RecentWorkout {
  id: number;
  name: string;
  startedAt: Date;
  completedAt: Date;
  totalVolume: number;
}

// In workoutService:
getRecentWorkouts(limit?: number): Promise<RecentWorkout[]>
startWorkoutFromTemplate(templateName: string): Promise<number>
```

From src/utils/formatters.ts (from Plan 01):
```typescript
export function formatRelativeDate(date: Date): string;
export function formatVolume(totalLbs: number): string;
```

Current HomePage.tsx structure:
```typescript
// Has: activeWorkout detection, handleStartWorkout, handleResumeWorkout
// Resume card at top if active, Start button below
// Need to add: RecentWorkouts component below the start button
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RecentWorkoutCard and RecentWorkouts components</name>
  <files>src/components/home/RecentWorkoutCard.tsx, src/components/home/RecentWorkouts.tsx</files>
  <read_first>src/services/workoutService.ts, src/utils/formatters.ts, src/db/models.ts</read_first>
  <action>
    Create src/components/home/ directory and two new components.

    **RecentWorkoutCard.tsx** (per D-17, D-18):
    - Props: `{ workout: RecentWorkout; onRepeat: (templateName: string) => void }`
    - Import RecentWorkout type from workoutService, formatRelativeDate and formatVolume from utils/formatters.
    - Layout: A card (bg-bg-card rounded-xl p-4) with flex row, items-center, min-h-[44px].
    - Left side (flex-1 min-w-0): workout.name as text-text-primary font-semibold truncate, below it a row with relative date (text-text-secondary text-sm) and formatted volume (text-text-secondary text-sm) separated by a middle dot.
    - Right side: "Repeat" button — text-accent font-semibold min-h-[44px] px-3 active:opacity-80 transition-opacity. onClick calls onRepeat(workout.name).
    - Touch target: button must be at least 44px tall per project constraints.

    **RecentWorkouts.tsx** (per D-05, D-06, D-16):
    - Import useLiveQuery from dexie-react-hooks.
    - Import workoutService and RecentWorkout type.
    - Import useNavigate from react-router.
    - Props: none (self-contained data fetcher).
    - Use useLiveQuery to call workoutService.getRecentWorkouts(10). This returns RecentWorkout[] or undefined while loading.
    - handleRepeat function: calls workoutService.startWorkoutFromTemplate(templateName), then navigates to `/workout/${workoutId}` per D-09.
    - Render: "Recent Workouts" heading (text-text-secondary text-sm font-semibold uppercase tracking-wide mb-3 mt-6), then a flex-col gap-2 of RecentWorkoutCard components.
    - If no recent workouts (empty array), render a subtle message: "Complete a workout to see it here" (text-text-secondary text-center text-sm py-4).
    - While loading (undefined), render nothing (avoid flash).

    Style with dark theme Tailwind tokens already established (bg-bg-card, text-text-primary, text-text-secondary, text-accent).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RecentWorkoutCard" src/components/home/RecentWorkoutCard.tsx
    - grep -q "onRepeat" src/components/home/RecentWorkoutCard.tsx
    - grep -q "formatRelativeDate" src/components/home/RecentWorkoutCard.tsx
    - grep -q "formatVolume" src/components/home/RecentWorkoutCard.tsx
    - grep -q "Repeat" src/components/home/RecentWorkoutCard.tsx
    - grep -q "useLiveQuery" src/components/home/RecentWorkouts.tsx
    - grep -q "getRecentWorkouts" src/components/home/RecentWorkouts.tsx
    - grep -q "startWorkoutFromTemplate" src/components/home/RecentWorkouts.tsx
    - grep -q "useNavigate" src/components/home/RecentWorkouts.tsx
    - grep -q "RecentWorkoutCard" src/components/home/RecentWorkouts.tsx
  </acceptance_criteria>
  <done>RecentWorkouts fetches recent workouts via useLiveQuery, renders RecentWorkoutCard for each. Repeat button calls startWorkoutFromTemplate and navigates to the new workout. Empty state handled.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate RecentWorkouts into HomePage</name>
  <files>src/pages/HomePage.tsx</files>
  <read_first>src/pages/HomePage.tsx, src/components/home/RecentWorkouts.tsx</read_first>
  <action>
    Modify HomePage.tsx to add the RecentWorkouts component below the Start/Start New Workout button. Per D-15, D-16, D-19:

    1. Import RecentWorkouts from '../components/home/RecentWorkouts'.
    2. After the Start Workout button (the last element currently), add `<RecentWorkouts />`.
    3. Keep everything else exactly as-is:
       - Resume Workout card at top when activeWorkout exists (D-19)
       - Start button styling (primary accent when no active, secondary when active) (D-15)
       - MOAR heading and tagline

    That's it — RecentWorkouts is self-contained (fetches its own data, handles its own navigation).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RecentWorkouts" src/pages/HomePage.tsx
    - grep -q "import.*RecentWorkouts" src/pages/HomePage.tsx
  </acceptance_criteria>
  <done>HomePage renders RecentWorkouts below the start button. Resume card still at top. Start button still prominent. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of home screen and repeat flow</name>
  <files>src/pages/HomePage.tsx, src/components/home/RecentWorkouts.tsx, src/components/home/RecentWorkoutCard.tsx</files>
  <action>
    Human verifies the complete Phase 3 flow works end-to-end on mobile. No code changes — verification only.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RecentWorkouts" src/pages/HomePage.tsx
    - grep -q "Repeat" src/components/home/RecentWorkoutCard.tsx
  </acceptance_criteria>
  <what-built>Recent Workouts list on home screen with editable workout names and one-tap Repeat functionality. Complete flow: start workout, name it, add exercises, log sets, finish, see it in recent list, tap Repeat to start a new session with same exercises.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open on mobile or mobile DevTools
    2. Start a new workout — verify the header name is editable (tap it, type "Push Day")
    3. Add 2-3 exercises, log a set for each
    4. Tap Finish
    5. You should be back on the home screen
    6. Verify "Recent Workouts" section appears below the Start button
    7. Verify the card shows: "Push Day", relative date ("Today"), and total volume
    8. Tap the "Repeat" button on the card
    9. Verify you land on the active workout screen with "Push Day" as the name and all 2-3 exercises pre-loaded as cards
    10. Verify the weight/reps from the previous session are pre-filled
    11. Start another new workout (don't name it) — finish it. Verify it appears with the auto-generated name "Workout - [date]"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User confirms: editable name works, recent workouts display correctly, Repeat creates new workout with exercises pre-loaded, pre-fill values from last session appear.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite passes
- HomePage shows RecentWorkouts component
- Recent workout cards display name, relative date, and volume
- Repeat button creates new workout from template and navigates to it
- All Phase 2 functionality preserved (resume, start, exercise entry)
</verification>

<success_criteria>
- Home screen shows last 10 completed workouts with name, date, volume
- Tapping Repeat creates a new workout with previous exercises pre-loaded
- User lands on active workout screen after Repeat with exercises as cards
- Start New Workout button remains prominent at top
- Resume Workout card still appears when active workout exists
</success_criteria>

<output>
After completion, create `.planning/phases/03-workouts-templates/03-03-SUMMARY.md`
</output>
