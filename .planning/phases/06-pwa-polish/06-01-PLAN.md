---
phase: 06-pwa-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - index.html
  - src/main.tsx
  - public/pwa-192.png
  - public/pwa-512.png
  - public/apple-touch-icon.png
  - src/components/pwa/InstallPromptBanner.tsx
  - src/pages/HomePage.tsx
autonomous: false
requirements: [PLT-03]

must_haves:
  truths:
    - "App can be added to home screen and launches standalone (no browser chrome)"
    - "App works fully offline after first load (service worker caches all assets)"
    - "App requests persistent storage on startup to prevent iOS Safari eviction"
    - "After first completed workout, user sees a subtle install prompt on the home page"
    - "Install prompt is dismissible and does not reappear once dismissed"
  artifacts:
    - path: "vite.config.ts"
      provides: "VitePWA plugin configuration with manifest and workbox settings"
      contains: "VitePWA"
    - path: "index.html"
      provides: "Apple mobile web app meta tags for iOS PWA support"
      contains: "apple-mobile-web-app"
    - path: "src/main.tsx"
      provides: "Service worker registration and persistent storage request"
      contains: "navigator.storage.persist"
    - path: "src/components/pwa/InstallPromptBanner.tsx"
      provides: "Conditional install prompt with iOS instructions"
      contains: "Add to Home Screen"
    - path: "public/pwa-192.png"
      provides: "192x192 PWA icon"
    - path: "public/pwa-512.png"
      provides: "512x512 PWA icon"
  key_links:
    - from: "vite.config.ts"
      to: "dist/sw.js"
      via: "VitePWA generates service worker at build time"
      pattern: "VitePWA"
    - from: "src/main.tsx"
      to: "navigator.storage"
      via: "persist() call on startup"
      pattern: "navigator\\.storage\\.persist"
    - from: "src/components/pwa/InstallPromptBanner.tsx"
      to: "src/services/workoutService.ts"
      via: "checks completed workout count to decide visibility"
      pattern: "getCompletedWorkouts"
---

<objective>
Configure the app as a Progressive Web App with add-to-home-screen support, offline caching via service worker, persistent storage to prevent iOS Safari eviction, and a conditional install prompt banner.

Purpose: This is the final phase — making MOAR a true home-screen app that works offline at the gym.
Output: Fully configured PWA with manifest, service worker, icons, iOS meta tags, persistent storage, and install prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pwa-polish/06-CONTEXT.md

@vite.config.ts
@index.html
@src/main.tsx
@src/App.tsx
@src/pages/HomePage.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: PWA infrastructure — manifest, service worker, icons, meta tags, persistent storage</name>
  <files>vite.config.ts, index.html, src/main.tsx, public/pwa-192.png, public/pwa-512.png, public/apple-touch-icon.png</files>
  <read_first>vite.config.ts, index.html, src/main.tsx, package.json</read_first>
  <action>
1. Install vite-plugin-pwa:
   npm install -D vite-plugin-pwa

2. Generate placeholder PWA icons (per D-07: orange "M" on dark background):
   - Create a simple SVG with an orange (#f97316) letter "M" on a dark (#0f0f0f) rounded-rect background
   - Use a Node script or canvas to export as PNG at 192x192 (public/pwa-192.png), 512x512 (public/pwa-512.png), and 180x180 (public/apple-touch-icon.png)
   - If PNG generation is complex, create SVG versions and use them directly — vite-plugin-pwa supports SVG icons
   - Alternative: use sharp or canvas npm package as a one-off script, then remove

3. Update vite.config.ts — add VitePWA plugin (per D-09, D-10, D-11, D-12, D-13):
   ```typescript
   import { VitePWA } from 'vite-plugin-pwa';
   ```
   Add VitePWA to plugins array with this configuration:
   - registerType: 'prompt' (per D-13: prompt user to reload on new version)
   - includeAssets: ['favicon.svg', 'apple-touch-icon.png']
   - manifest:
     - name: 'MOAR' (per D-01)
     - short_name: 'MOAR' (per D-02)
     - theme_color: '#f97316' (per D-03)
     - background_color: '#0f0f0f' (per D-04)
     - display: 'standalone' (per D-05)
     - orientation: 'portrait' (per D-06)
     - start_url: '/'
     - icons: array with 192x192 and 512x512 entries (purpose: 'any maskable')
   - workbox:
     - globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'] (per D-12: precache all built assets)
     - runtimeCaching: [] (per D-11: no network-first needed, no backend API)

4. Update index.html — add iOS-specific meta tags (per D-08):
   Inside <head>, add:
   ```html
   <meta name="theme-color" content="#f97316" />
   <meta name="apple-mobile-web-app-capable" content="yes" />
   <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
   <meta name="apple-mobile-web-app-title" content="MOAR" />
   <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
   ```

5. Update src/main.tsx — add persistent storage request (per D-14, D-15):
   After createRoot().render(), add:
   ```typescript
   // Request persistent storage (per D-14, D-15: don't block, just request silently)
   if (navigator.storage && navigator.storage.persist) {
     navigator.storage.persist();
   }
   ```
   Note: Do NOT await the result — fire and forget (per D-15).

6. Verify build produces service worker:
   Run `npm run build` and confirm dist/ contains sw.js (or service-worker.js) and manifest.webmanifest.
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npm run build 2>&1 | tail -5 && ls dist/sw.js dist/manifest.webmanifest 2>/dev/null && echo "PWA BUILD OK" || echo "PWA BUILD FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains "VitePWA" import and plugin call
    - vite.config.ts manifest has name "MOAR", theme_color "#f97316", display "standalone"
    - index.html contains "apple-mobile-web-app-capable" meta tag
    - index.html contains "apple-mobile-web-app-status-bar-style" with "black-translucent"
    - index.html contains theme-color meta tag with "#f97316"
    - src/main.tsx contains "navigator.storage.persist"
    - public/pwa-192.png exists (or SVG equivalent referenced in manifest)
    - public/pwa-512.png exists (or SVG equivalent referenced in manifest)
    - npm run build succeeds and dist/ contains sw.js and manifest.webmanifest
  </acceptance_criteria>
  <done>Build produces a valid PWA with service worker, manifest, and icons. iOS meta tags present. Persistent storage requested on startup.</done>
</task>

<task type="auto">
  <name>Task 2: Install prompt banner — conditional display after first workout, iOS instructions, dismissible</name>
  <files>src/components/pwa/InstallPromptBanner.tsx, src/pages/HomePage.tsx</files>
  <read_first>src/pages/HomePage.tsx, src/services/workoutService.ts, src/components/home/RecentWorkouts.tsx</read_first>
  <action>
1. Create src/components/pwa/InstallPromptBanner.tsx (per D-17, D-18, D-19, D-20):

   Component logic:
   - On mount, check localStorage for 'moar-install-dismissed'. If 'true', render nothing.
   - Check if app is already in standalone mode: window.matchMedia('(display-mode: standalone)').matches || (window.navigator as any).standalone. If standalone, render nothing.
   - Use Dexie useLiveQuery to count completed workouts (workouts where completedAt is not null). If count === 0, render nothing (per D-17: don't show on first visit).
   - Only show when: not dismissed AND not standalone AND has at least 1 completed workout.

   Detect platform:
   - const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent)
   - const isAndroid = /Android/.test(navigator.userAgent)

   UI (Tailwind dark theme, consistent with app):
   - Container: bg-[#1a1a1a] border border-[#2a2a2a] rounded-xl p-4 mx-4 mb-4
   - Text: text-sm text-gray-300
   - For iOS (per D-20): "Add MOAR to your home screen: tap the Share button, then 'Add to Home Screen'"
   - For Android/desktop with beforeinstallprompt support: "Install MOAR for the best experience" with an "Install" button that calls the stored beforeinstallprompt event
   - For Android/desktop without beforeinstallprompt: "Add MOAR to your home screen from your browser menu"
   - Dismiss button: small "x" or "Dismiss" text button, top-right. On tap, set localStorage 'moar-install-dismissed' = 'true'.

   For Android beforeinstallprompt:
   - Listen for 'beforeinstallprompt' event on window in useEffect
   - Store the event in a ref
   - On "Install" button click, call event.prompt()

2. Wire into HomePage.tsx:
   - Import InstallPromptBanner
   - Render it BELOW the main content (after RecentWorkouts), so it doesn't push primary UI down
   - The banner handles its own visibility logic — just render it unconditionally and let it self-hide

3. The banner should feel helpful, not nagging (per specifics). Keep copy friendly and brief.
  </action>
  <verify>
    <automated>cd "C:/Users/Dan Walsh/Desktop/DW/MOAR" && npx tsc --noEmit 2>&1 | tail -10 && echo "TYPE CHECK OK" || echo "TYPE CHECK FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pwa/InstallPromptBanner.tsx exists and exports a default component
    - InstallPromptBanner checks localStorage for 'moar-install-dismissed'
    - InstallPromptBanner checks display-mode standalone
    - InstallPromptBanner checks completed workout count > 0 before showing
    - InstallPromptBanner contains iOS-specific text mentioning "Share" and "Add to Home Screen"
    - InstallPromptBanner has a dismiss mechanism that writes to localStorage
    - src/pages/HomePage.tsx imports and renders InstallPromptBanner
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Install prompt banner shows on HomePage after first completed workout, provides iOS-specific instructions, and is permanently dismissible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify PWA installability and offline support</name>
  <action>Human verification of PWA build: manifest, service worker, install prompt banner visibility and dismissal. See how-to-verify steps.</action>
  <what-built>Complete PWA configuration: manifest, service worker, icons, iOS meta tags, persistent storage request, and conditional install prompt banner.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview` to serve the production build
    2. Open http://localhost:4173 in Chrome
    3. Open DevTools > Application tab:
       - Check "Manifest" section shows MOAR with correct theme_color (#f97316) and icons
       - Check "Service Workers" section shows an active service worker
    4. In Chrome DevTools > Application > Storage, verify the app would work offline (all assets cached)
    5. On the HomePage, if you have completed workouts, verify the install prompt banner appears
    6. Click dismiss on the banner — refresh the page — verify it does not reappear
    7. (Optional) Test on iPhone Safari: verify apple-mobile-web-app meta tags render correctly, Share > Add to Home Screen works
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds with `npm run build` and produces sw.js + manifest.webmanifest in dist/
- TypeScript compiles with no errors (`npx tsc --noEmit`)
- Chrome DevTools Application tab shows valid manifest and active service worker
- Install prompt banner only appears after at least one completed workout
- Install prompt is permanently dismissible via localStorage flag
</verification>

<success_criteria>
1. App can be added to home screen on both iOS Safari and Android Chrome
2. App works fully offline after first load (service worker precaches all assets)
3. Persistent storage is silently requested on startup
4. Install prompt appears only after first completed workout, with iOS-specific instructions
5. Install prompt does not reappear once dismissed
</success_criteria>

<output>
After completion, create `.planning/phases/06-pwa-polish/06-01-SUMMARY.md`
</output>
